# Current WIP

Canonical status file: `tasks/wip.md`

Quick snapshot:
- Program is in tools migration execution (Option 3).
- `mp1-tools` Phase 2 is complete (`mlp_tools` namespace, `mp1-vasp` added, pins rolled out).
- Next focus is Phase 3 Artemis migration into `mp1-tools`.

If resuming a new session:
1. Read `tasks/wip.md`.
2. Read `legacy_processing/01-tools/execution_checklist.md`.
3. Resume from the latest unchecked item in `legacy_processing/01-tools/tools_migration_work_order.md`.
